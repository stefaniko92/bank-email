specVersion: "v1"
serviceId: "bank-email"
location: "europe-central2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bank-email-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
